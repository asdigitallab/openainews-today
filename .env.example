# Only needed if you want LIVE machine-written signals (otherwise the offline
# bank is used automatically). Set this in Vercel: Settings > Environment Variables.
ANTHROPIC_API_KEY=

# Optional: point the frontend at a different generation endpoint.
# Defaults to /api/generate (the included Vercel function).
# VITE_GENERATE_URL=
